#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    if col != row[-1]
    for row in matrix:
        for col in row:
            print("{:d}".format(col), end=" ")
        else:
            print()
